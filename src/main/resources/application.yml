spring:
  h2:
    console:
      enabled: true
  main:
    allow-bean-definition-overriding: true
  jpa:
    show-sql=true:
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
